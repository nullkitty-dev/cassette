<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 520 210" width="520" height="210" role="img" aria-labelledby="title desc">
  <title id="title">c(a)ssette</title>
  <desc id="desc">A retro cassette inlay label. Three colour bands above the wordmark c(a)ssette, over the line CSS parser, zero deps, JVM.</desc>
  <defs>
    <linearGradient id="paper" x1="0" y1="0" x2="0.15" y2="1">
      <stop offset="0" stop-color="#FFFFFF"/>
      <stop offset="0.45" stop-color="#FCF7EA"/>
      <stop offset="1" stop-color="#F3E9D2"/>
    </linearGradient>
    <filter id="grain" x="0" y="0" width="100%" height="100%">
      <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="3" stitchTiles="stitch" result="n"/>
      <feColorMatrix in="n" type="saturate" values="0"/>
    </filter>
    <filter id="lift" x="-20%" y="-20%" width="140%" height="150%">
      <feDropShadow dx="0" dy="4" stdDeviation="5" flood-color="#2C2219" flood-opacity="0.28"/>
    </filter>
    <clipPath id="labelClip">
      <rect x="10" y="8" width="500" height="186" rx="12"/>
    </clipPath>
  </defs>

  <!-- label -->
  <g filter="url(#lift)">
    <rect x="10" y="8" width="500" height="186" rx="12" fill="url(#paper)"/>
  </g>

  <g clip-path="url(#labelClip)">
    <!-- three colour bands, closed by a teal rule -->
    <rect x="10" y="8" width="500" height="13" fill="#C0472F"/>
    <rect x="10" y="21" width="500" height="13" fill="#E4703A"/>
    <rect x="10" y="34" width="500" height="13" fill="#E9B44C"/>
    <rect x="10" y="47" width="500" height="3" fill="#2E7F76"/>

    <!-- footer stripes, mirroring the top block -->
    <rect x="10" y="184" width="500" height="5" fill="#E9B44C"/>
    <rect x="10" y="189" width="500" height="5" fill="#C0472F"/>

    <!-- paper grain -->
    <rect x="10" y="8" width="500" height="186" filter="url(#grain)" opacity="0.08"/>
  </g>

  <!-- wordmark -->
  <g font-family="Futura, Avenir Next, Century Gothic, Trebuchet MS, Helvetica Neue, Arial, sans-serif" text-anchor="middle">
    <text x="260" y="130.208" font-size="78" font-weight="700" fill="#2C2219" letter-spacing="-1.5">c<tspan fill="#E4703A">(</tspan>a<tspan fill="#E4703A">)</tspan>ssette</text>
    <text x="260" y="171.616" font-size="15" font-weight="600" fill="#2E7F76" textLength="390" lengthAdjust="spacing">CSS PARSER &#183; ZERO DEPS &#183; JVM</text>
  </g>

  <rect x="10" y="8" width="500" height="186" rx="12" fill="none" stroke="#2C2219" stroke-opacity="0.16" stroke-width="1.5"/>
</svg>
